public enum ResourceType
{
    Grain,    
    Flour,
    Bread
}

public enum LocationType
{
    Producer,
    Processor,
    Town
}

public enum ContractBreakType
{
    Supplier,
    Destination,
    Shipper
}